[env]

[tools]
bun = "latest"
node = "latest"

[tasks.generate]
description = "Generate all cheatsheets (text and PNG) from TOML data"
run = "bun scripts/generate.ts"

[tasks.lint]
description = "Lint and type check code"
run = ["bun eslint scripts/"]
